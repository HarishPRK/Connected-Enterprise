// The first assignment for a fresh registration. Later assignments increment the
// gateway's stored generation; existing registrations keep their current value.
export const INITIAL_ONBOARDING_GENERATION = 5;
